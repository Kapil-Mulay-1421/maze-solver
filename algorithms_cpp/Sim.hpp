#ifndef SIM_HPP
#define SIM_HPP

class Sim {
public:
    bool moveForward(double distance = 0.25) {
        // Stubbed simulation: always return success
        return true;
    }

    bool turnLeft() {
        // Stubbed simulation: always return success
        return true;
    }

    bool turnRight() {
        // Stubbed simulation: always return success
        return true;
    }
};

#endif // SIM_HPP
